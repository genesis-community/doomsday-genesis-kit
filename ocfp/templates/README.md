The files in this directory were removed, and the functionality they provided
was replaced by data structures in `hooks/blueprint.pm`.  See the following methods
for more modifying the doomsday plan contents:
- `_create_credhub_content` - replaces `credhub.yml`
- `_create_vault_content` - replaces `vault.yml`
- `_create_vault_ext_content` - replaces `vault_ext.yml`
- `_create_fqdns_content` - replaces `fqdns.yml`
