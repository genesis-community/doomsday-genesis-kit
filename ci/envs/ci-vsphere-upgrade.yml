---
kit:
  features:
    - ((append))
    - userpass
    - tls
  ci:
    job: upgrade
    type: upgrade
    serial_groups:
      - deployment

genesis:
  env:   ci-vsphere-upgrade

params:
  vm_type: small
